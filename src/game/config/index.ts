// Game-specific config (extends scaffold)
export {
  type Environment,
  getEnvironment,
  isLocal,
  isProduction,
  getLocalAssetPath,
  getCdnUrl,
  getChaptersUrl,
  getLevelsUrl,
  resolveLevelUrl,
} from './environment';
