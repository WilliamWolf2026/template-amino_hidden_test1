/**
 * Game-specific types — Template Skeleton
 *
 * Define your game's domain types here (level format, game objects, etc.)
 * Template-level types (GameData, DialogueMessage) live in ~/game/types/.
 */
