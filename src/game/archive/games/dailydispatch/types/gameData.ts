/**
 * Re-export from template-level types.
 * These types are consumed by both template code and citylines code.
 */
export type {
  TexturePackRef,
  CountyRef,
  StoryRef,
  ClueRef,
  LevelRef,
  ChapterRef,
  GameData,
  GameIndexEntry,
  GamesIndex,
} from '~/game/types/gameData';
