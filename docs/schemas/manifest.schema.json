{
  "$schema": "http://json-schema.org/draft-07/schema#",
  "title": "Asset Manifest",
  "description": "Schema for the game asset manifest. See docs/core/manifest-contract.md.",
  "type": "object",
  "required": ["cdnBase", "bundles"],
  "properties": {
    "cdnBase": {
      "type": "string",
      "description": "Base URL or path for assets (e.g. /assets or https://cdn.example.com/game/assets)"
    },
    "localBase": {
      "type": "string",
      "description": "Optional fallback path if CDN is unavailable"
    },
    "bundles": {
      "type": "array",
      "items": { "$ref": "#/$defs/ManifestBundle" },
      "description": "List of asset bundles; bundle names must be unique"
    }
  },
  "$defs": {
    "ManifestBundle": {
      "type": "object",
      "required": ["name", "assets"],
      "properties": {
        "name": {
          "type": "string",
          "pattern": "^[a-z][a-z0-9-]*$",
          "description": "Unique bundle name; prefer reserved prefix (boot-, theme-, audio-, core-, scene-, fx-, defer-, data-)"
        },
        "assets": {
          "type": "array",
          "items": { "type": "string" },
          "description": "Paths relative to cdnBase; no leading slash, no .., no absolute URLs"
        },
        "target": {
          "type": "string",
          "enum": ["dom", "gpu", "agnostic"],
          "description": "Optional loader target override; otherwise inferred from bundle name prefix"
        },
        "kind": {
          "type": "string",
          "enum": ["boot", "theme", "audio", "data", "core", "scene", "fx", "defer"],
          "description": "Optional semantic classification. When present, overrides prefix-based inference for validation. If both kind and a name prefix are present, they must be consistent."
        }
      }
    }
  }
}
